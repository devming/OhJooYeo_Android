<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.ohjooyeo.ohjooyeoandroid">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".components.main.phrase.view.PhraseDetailActivity">
        </activity>
        <activity android:name=".components.advertisement.view.AdvertisementActivity">
        </activity>
        <activity android:name=".components.more.view.MoreActivity">
        </activity>
        <activity android:name=".components.main.history.view.HistoryActivity">
        </activity>
        <activity android:name=".components.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>